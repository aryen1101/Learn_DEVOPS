This code keeps asking the user to enter a number.
If the user enters `q`, the program stops running.
If the user enters anything other than a number, it shows an error message.
For a valid number, it prints the number entered by the user.
The loop continues until the user chooses to quit by entering `q`.
